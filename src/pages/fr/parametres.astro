---
import PageLayout from '@/layouts/PageLayout.astro';
import LangSwitcher from '@/components/LangSwitcher';
import ThemeSwitcher from '@/components/ThemeSwitcher';
import { settingsData } from './data/settings.astro';

const { title, description, lang, alternates } = settingsData
---

<PageLayout title={title} description={description} lang={lang} alternates={alternates}>
  <h1>{title}</h1>
  <ul>
    <LangSwitcher client:load />
    <ThemeSwitcher client:load labels={{ theme: "Thème", color: "Couleur", light: "Clair", dark: "Sombre" }}/>
  </ul>
</PageLayout>
