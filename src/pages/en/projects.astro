---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { projectsData } from './data/projects.astro';

const { title, description, lang, alternates } = projectsData
---


<BaseLayout title={title} description={description} lang={lang} alternates={alternates}>
  <h1>Settings</h1>
  <ul>
    <li>Language: <strong>Fr / En</strong> (to be implemented)</li>
    <li>Theme: <strong>Light / Dark / Fun</strong> (to be implemented)</li>
    <li>Primary color: <strong>Option 1 / 2 / 3</strong> (to be implemented)</li>
  </ul>
</BaseLayout>
